# RCON Warfare — диспетчерская боевой техники для Minecraft

Управление боевой техникой (самолёты, вертолёты, БПЛА, танки, транспорт) на
сервере Minecraft через RCON: тактическая карта со сканом рельефа, планировщик
маршрутов с действиями в точках, боты с собственными маршрутами, оружие
с боезапасом и топливом.

Дефекты — в [`BUGS.md`](BUGS.md), история версий — в
[`CHANGELOG.md`](CHANGELOG.md), знания про Minecraft/RCON — в
[`SKILL.md`](SKILL.md), устройство и инварианты — в
[`ARCHITECTURE.md`](ARCHITECTURE.md), состояние и план — в
[`HANDOFF.md`](HANDOFF.md).

**Целевая версия сервера: Minecraft 1.20.1+ (vanilla).** Поддержка 1.21.2+
(новый синтаксис NBT) заложена через диалекты; определяется автоматически
зондами при подключении.

---

## Текущее состояние

Версия **15.2.0**. Готово и покрыто тестами ядро и интерфейс: сеть, состояние
мира, сканер рельефа, трекер игроков, слой команд Minecraft, физика техники,
маршруты и ИИ, оружие, базы и экономика, карта на Dear PyGui, имитатор
сервера для офлайн-разработки.

```
✅ работает      rcon · mc · world · events · config · geometry · tracker
                 scanner · model · units · weapons · engine · routes · ai
                 storage · presets · progress · settings · sound · gamefx
                 maprender · ui (Dear PyGui) · app · main (CLI)
📝 исключено     посадка пилота в кабину (GAME-01/02) — решение заказчика
⬜ открыто       долг перед v16 — раздел 9 в ARCHITECTURE.md
```

**596 тестов, все зелёные.** GL-контекст для тестов ядра не нужен:

```bash
pip install -r requirements.txt
python main.py --selftest        # все тесты (596)
python -m pytest tests/ -q       # то же через pytest
python main.py --mock            # офлайн-демо: имитатор сервера, весь тракт
python main.py gui               # диалог настройки (Tkinter) -> диспетчерская
python main.py --mission 40      # демо: маршруты, ИИ, библиотека JSON
python tools/screenshot.py       # headless-скриншот карты -> artifacts/
RWF_PROFILE=1 python main.py gui # профиль фаз кадра раз в 30 кадров
```

Закрыто **141 из 143** дефектов наброска v10 плюс все дефекты v12–v15
(разделы N в `BUGS.md`); две позиции исключены решением заказчика.
Скриншоты интерфейса — в `artifacts/`.

---

## Быстрый старт (без сервера Minecraft)

Встроенный имитатор сервера позволяет прогнать весь тракт целиком:

```bash
pip install -r requirements.txt
python main.py --mock                 # подключение -> зонд -> игроки -> рельеф -> очередь
python main.py --flight               # полёт самолёта, огонь, деспаун (сквозное демо)
python main.py --mock --scan 128 8    # скан рельефа + ASCII-карта
python main.py --mock --watch 10      # 10 секунд следить за игроками
python main.py --mock --bench         # пакетная отправка vs поштучная
python main.py --probe                # только опрос возможностей сервера
```

Пример вывода `--flight`:

```
=== Полёт по курсам (автопилот) ===
  курс  90.0° -> факт   85.4°  V= 36.5 м/с  H= 180.0 м  n=1.02  топливо 98%
  курс 180.0° -> факт  175.5°  V= 36.5 м/с  H= 180.0 м  n=1.02  топливо 97%
  блоков в мире после 16 с полёта: 25 (модель = 25)
[+] Следа нет: в мире ровно блоки текущей модели

=== Огонь ===
  подвесов сработало: пушка=1 НАР=2 бомбы=1
  боезапас: 126/140

=== Деспаун ===
  блоков в мире после снятия юнита: 0
[+] Мир чист: модель убрана полностью, ландшафт не тронут
```

Пример вывода `--mock`:

```
[+] Имитатор сервера поднят на 127.0.0.1:25575 (версия 1.20.1, vanilla)
[+] RCON подключён к 127.0.0.1:25575 (пул 6 соединений)
[+] vanilla 1.20.1 | backend=display | display-сущности, marker, interaction, /damage

=== Игроки ===
  Arlik88      X=    32.6 Y=  76.0 Z=     0.2
  Steve        X=   -16.6 Y=  63.0 Z=    28.1

=== Рельеф ===
  [+] 289 тайлов / 289 колонок за 0.1 с (4643 команд, 82 сетевых захода)
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~:::::::::~~~~~~~~~~~~~~~~~~~~~~~~
...
X: -32..96   Z: -64..64   Y: 62..91   шаг: 8

=== Очередь команд (визуальные кадры сливаются) ===
  отправлено 40 кадров модели -> на сервер ушло 1 (слито 39)
```

## Подключение к настоящему серверу

В `server.properties`:

```properties
enable-rcon=true
rcon.port=25575
rcon.password=ваш_пароль
```

Затем:

```bash
python main.py --probe --host 127.0.0.1 --port 25575 --password 2203
```

Настройки сохраняются в `rwf.json` (создаётся при первом сохранении) и
переопределяются переменными окружения: `RWF_HOST`, `RWF_PORT`,
`RWF_PASSWORD`, `RWF_MOCK`, `RWF_TICK`, `RWF_CONFIG`. Прежние имена
`BOMBER_*` продолжают приниматься как устаревший псевдоним.

---

## Архитектура

Подробно — в [`ARCHITECTURE.md`](ARCHITECTURE.md): слои, инварианты, модель
потоков, грабли Dear PyGui, точки расширения и известный долг. Кратко:

```
rwf/                     ЯДРО — не знает про Dear PyGui, связь через EventBus
├── config.py       настройки: rcon/sim/combat/map/scanner/ui, JSON + окружение
├── events.py       EventBus — ядро публикует, UI подписывается
├── geometry.py     углы и векторы (один источник правды)
├── rcon.py         Source RCON, пул с арендой, CommandQueue с приоритетами
├── mc.py           построители команд, ServerCaps (зонды версий), парсеры
├── world.py        World: игроки, юниты, маршруты, маркеры, зоны, рельеф
├── tracker.py      PlayerTracker — пакетный опрос игроков
├── scanner.py      TerrainScanner — пакетное зондирование рельефа
├── model.py        чертежи техники и блочная модель с дифф-обновлением
├── units.py        UnitSpec и физика (самолёт/вертолёт/БПЛА/танк/транспорт)
├── weapons.py      каталог оружия, боезапас, баллистика, УР с наведением
├── missiles.py     блочные ракеты, наведение, детонация
├── combat.py       урон, броня, кил-фид, огонь из игры
├── manpads.py      ПЗРК игрока
├── engine.py       тик-цикл, жизненный цикл, дежурство, логистика
├── routes.py       точки, действия, исполнитель маршрута
├── ai.py           боты по фазам захода, гибридный перепланировщик
├── bases.py        базы, стоянки, RecoveryNode, экономика снабжения
├── groundnav.py    навигация наземной техники по рельефу
├── wreckage.py     следы аварий: кратер, остов, огонь
├── unitstate.py    «техника → слово» для интерфейса
├── storage.py      библиотека маршрутов в JSON
├── presets.py      пресеты запуска (с кэшем каталога)
├── progress.py     очки, звания, достижения оператора
├── settings.py     UI-настройки между запусками
├── sound.py        синтез WAV, бэкенды pygame/CLI/null
├── gamefx.py       игровая обратная связь: чат, actionbar, звуки
├── icons.py        силуэты техники с LOD
├── uicons.py       векторные иконки интерфейса
├── maprender.py    трансформация камеры, слои, растр рельефа (без DPG)
├── app.py          Application — композиционный корень
├── mock_server.py  имитатор Minecraft для тестов и офлайн-демо
└── ui/             Dear PyGui: цикл, STATE, проекция, карта, ПКМ-меню, тема

tests/               596 тестов (pytest/unittest), GL нужен только UI-тесту
tools/screenshot.py  headless-скриншот карты для отчётов и регрессий
main.py              CLI: demo / flight / mission / selftest / probe / watch / scan / bench / gui
```

### Правила, которые нельзя нарушать

1. **Ядро не знает про Dear PyGui.** Общение только через `EventBus`. Мутация
   виджетов из фоновых потоков запрещена (дефект UI-01 в наброске).
2. **Ничего не уходит на сервер из колбэка интерфейса.** Только `CommandQueue`
   или фоновый воркер. Иначе кадр замерзает на время RTT (RCON-06).
3. **`snapshot()` возвращает копии.** UI может перебирать снимок, пока физика
   меняет объекты (WORLD-02).
4. **Команды Minecraft строятся в `mc.py` и возвращают строку.** Это позволяет
   тестировать их сравнением строк, без сервера.
5. **Идентификатор юнита присваивается синхронно, до запуска потока** (UI-02).
6. **Каждый фикс сопровождается тестом.** Без теста статус в `BUGS.md` не
   меняется на `✅ исправлено`.

### Поток данных

```
        Minecraft-сервер
              ↑ ↓  RCON (пул соединений, пакетная отправка)
        CommandQueue ← приоритеты: CRITICAL / NORMAL / VISUAL(слияние)
              ↑
   ┌──────────┴───────────┐
   │  World (состояние)   │ ← PlayerTracker, TerrainScanner
   └──────────┬───────────┘
              ↑ snapshot() (копии, под локом)
     Units ⇄ Routes ⇄ AI          EventBus ──→ MSG_Q ──→ STATE (главный поток)
              ↓
        MapRenderer → примитивы → painter → drawlist пера карты
```

---

## Ключевые инженерные решения

### 1. Модель техники — блоки (решение заказчика)

Рассматривались два подхода:

| | Блоки (`setblock`) | Display-сущности (1.19.4+) |
|---|---|---|
| Команд на кадр | ~2 × N_блоков | 1 `tp` |
| Разрушает ландшафт | риск (см. ниже) | нет |
| Вид в мире | «настоящая» конструкция из блоков | блоки, парящие в воздухе |
| Совместимость | любая версия | 1.19.4+ |

Выбран **блочный** — как в исходном проекте. Возможности сервера всё равно
определяются зондом (`ServerCaps.model_backend`), поэтому при желании стратегию
можно заменить, не переписывая юниты: модель изолирована в `model.py`.

Набросок рисовал технику блоками так, что это ломало мир: `clear_model()`
вызывался для НОВОЙ позиции (старая не убиралась → след из блоков через всю
карту), а вокруг модели выполнялся `fill x±6 y-2..y+3 z±6 air replace`,
стиравший ландшафт и постройки в объёме 13×6×13 каждый тик. Как это решено —
в разделе 5.

### 2. Сканер рельефа — пакетное зондирование по уровням

Вместо 56 последовательных запросов на колонку — одна высота опрашивается сразу
у всех колонок одним `run_many`. Число сетевых заходов перестаёт зависеть от
числа колонок.

Замер (имитатор, localhost):

| Область | Колонок | Команд | Сетевых заходов | Время |
|---------|:-------:|:------:|:---------------:|:-----:|
| r=96, шаг 8 | 625 | ~10 000 | ~90 | 0.2 с |
| r=128, шаг 8 | 1 089 | 17 239 | 144 | 0.3 с |
| r=256, шаг 8 | 4 225 | 67 645 | **544** | **1.3 с** |

Набросок на той же области r=256/шаг 8 потребовал бы ~279 000 сетевых заходов
(4225 колонок × ~66 запросов). Пакетная отправка команд отдельно даёт 3.7×
на localhost (`python main.py --mock --bench`) и на порядок больше на
удалённом сервере, где выигрывается RTT.

Многопоточность не используется намеренно: сервер исполняет команды в одном
потоке, параллельные соединения дают только гонки (SCAN-02).

### 3. Определение версии — зондами, а не строкой

У vanilla нет команды `version`. Вместо парсинга строки проверяем возможности:
неизвестный тип сущности или команда дают `Unknown or incomplete command`,
известные — `Test failed` / `No entity was found`. Зонды не имеют побочных
эффектов (пустая выборка по несуществующему тегу), это проверяется тестом.

### 4. Очередь команд с приоритетами и слиянием

| Приоритет | Содержимое | При перегрузке |
|-----------|-----------|----------------|
| CRITICAL (0) | взрывы, спавн, удаление юнита | не выбрасывается |
| NORMAL (10) | выстрелы, tp, служебное | не выбрасывается |
| VISUAL (20) | кадры модели | сливается по ключу, выбрасывается первым |

Плюс rate-limit (по умолчанию 250 команд/с), чтобы не положить сервер.

### 5. Модель из блоков — с учётом каждого своего блока

По выбору заказчика техника рисуется блоками, а не display-сущностями. Чтобы
это не уничтожало мир и не грузило сервер, модель:

* описана декларативно (клетки `fwd/right/up/block`), углы применяются при
  проекции — один чертёж корректен под любым yaw/pitch/roll;
* **помнит каждый поставленный ею блок** и убирает только свои: никакого
  `fill ... air` по чужому ландшафту;
* обновляется **диффом**: на кадр уходят только изменившиеся клетки, повтор
  той же позы — ноль команд;
* рисуется не чаще `model_update_hz` (по умолчанию 5 Гц).

Бюджет: `2 × N_блоков × model_update_hz` команд/с на движущийся юнит.
Су-25 (22 блока) при 5 Гц ≈ 220 команд/с. Команды идут приоритетом VISUAL
с общим ключом на клетку, поэтому при перегрузке очередь отбрасывает
устаревшие кадры, а не важные команды.

Отдельная ловушка, которую поймал тест: снятие модели идёт приоритетом
CRITICAL, а отрисовка — VISUAL. Без общего ключа «убрать блок» обгоняло в
очереди ещё не отправленное «поставить блок», и блок оставался в мире
навсегда (NEW-01).

### 6. Управление — абсолютные цели, физика — согласованный вираж

Все значения управления (`set_target`) — абсолютные цели, физика приводит к ним
с ограниченной скоростью. Вираж считается по-настоящему: n = 1/cos(крен),
ω = g·tan(крен)/V, поэтому перегрузка и скорость разворота получаются из
крена и скорости, а не назначаются произвольно. Автопилот курса идёт в обратную
сторону: ошибка курса → желаемая ω → требуемый крен φ = atan(ω·V/g).

В наброске ручной крен и автопилот правили `self.roll` по очереди в пределах
одного тика и гасили друг друга — самолёт не выходил на курс (NEW-02).

### 7. Офлайн-разработка

`MockMCServer` реализует протокол RCON, синтетический рельеф, двигающихся
игроков и ведёт `command_log`. Тесты проверяют не только «не упало», но и
что именно ушло на сервер: сколько команд на кадр модели, не превышен ли
лимит `fill` в 32768 блоков, не задублировался ли взрыв.

---

## План

Итерации v11–v15 закрыты: ядро, физика, маршруты, ИИ, карта, интерфейс,
геймификация. Текущая версия — **15.2.0** (приведение в порядок: см.
`CHANGELOG.md`).

### v16 — 3D-карта, воксельный мир, обновлённый UI

Заявлено заказчиком. Что из текущей архитектуры этому помогает и что мешает —
раздел 9 в [`ARCHITECTURE.md`](ARCHITECTURE.md). Порядок выбран так, чтобы
каждый шаг давал работающий и проверяемый результат:

1. **База** — закрыть долг перед v16: граница «чистые данные» для тактики
   (`tac_apply` не читает STATE из воркера), `test_perf.py` с порогами,
   `pyproject.toml` + зависимости CI.
2. **3D-камера** — матрицы в `geometry.py`, проекция мира в примитивы;
   `painter` остаётся 2D-растеризатором кадра, ядро не трогается.
3. **Воксельный мир** — расширение `TerrainRaster` по Y вместо нового
   источника данных; слои карты получают высоту.
4. **UI** — переработка по итогам UX-аудита.

### Кандидат в v16: скан файла мира вместо (или вместе с) RCON-зондированием

Заказчик предложил читать сейв мира напрямую. Это снимает главное узкое место
`scanner.py`: скан через RCON — это десятки тысяч сетевых заходов (SCAN-03),
а region-файлы дают те же колонки рельефа локально и мгновенно.

Порядок работы (перед реализацией — исследование, не изобретать формат):
1. Выбрать библиотеку вместо своего парсера Anvil/NBT. Кандидаты для разбора:
   `anvil-parser2` (форк поддерживаемого `anvil-parser`), `nbtlib` (NBT +
   ручной разбор region), `amulet-core` (тяжёлый, но покрывает версии).
   Критерии: поддержка 1.20.1 и 1.21.2, лицензия, живость, вес.
2. Проверить, что путь к сейву вообще доступен оператору (локальный сервер;
   для удалённого — SFTP/копия каталога).
3. Источник рельефа становится вторым провайдером: `TerrainRaster` не должен
   знать, откуда пришли колонки (сейчас их даёт `TerrainGrid`).
4. Высоты/биомы/блоки из чанков — сверить с тем, что отдаёт RCON-скан
   (тест на согласованность на одном и том же мире).


---

## Состояние модулей

| Модуль | Назначение | Тесты |
|--------|-----------|-------|
| `config.py` | настройки, файл `rwf.json`, переменные `RWF_*` | `test_config.py` |
| `events.py` | `EventBus` — единственный канал ядро → UI | `test_world.py` |
| `geometry.py` | углы и векторы: один источник правды | через потребителей |
| `rcon.py` | Source RCON: пул, очередь, приоритеты | `test_rcon.py` |
| `mock_server.py` | имитатор Minecraft для офлайна | `test_rcon.py`, `test_mc.py` |
| `mc.py` | команды Minecraft, зонды версии | `test_mc.py` |
| `world.py` | состояние мира, снимки | `test_world.py` |
| `scanner.py` | пакетное зондирование рельефа | `test_tracker_scanner.py` |
| `tracker.py` | игроки на карте | `test_tracker_scanner.py` |
| `model.py` | блочная модель, дифф | `test_model.py` |
| `units.py` | физика техники | `test_units.py`, `test_transport_ground.py` |
| `weapons.py` | каталог оружия, баллистика | `test_weapons.py` |
| `missiles.py` | блочные ракеты | `test_combat.py` |
| `combat.py` | урон, броня, кил-фид | `test_combat.py` |
| `manpads.py` | ПЗРК игрока | `test_manpads.py` |
| `engine.py` | тик-цикл, жизненный цикл | `test_engine.py`, `test_v15_engine.py` |
| `routes.py` | waypoints, действия, исполнитель | `test_routes.py` |
| `ai.py` | боты (Strike/Fighter/Kamikaze/…) | `test_ai.py` |
| `bases.py` | базы, стоянки, экономика | `test_carrier.py`, `test_combat.py` |
| `groundnav.py` | навигация по рельефу | `test_v15_domain.py` |
| `wreckage.py` | кратер, остов, огонь | `test_v15_domain.py` |
| `unitstate.py` | «техника → слово» | `test_v15_domain.py` |
| `storage.py` | библиотека маршрутов (JSON) | `test_storage.py` |
| `presets.py` | пресеты запуска (кэш каталога) | `test_v15_progress.py` |
| `progress.py` | очки, звания, достижения | `test_v15_progress.py` |
| `settings.py` | UI-настройки (JSON) | `test_settings.py` |
| `sound.py` | синтез WAV, бэкенды | `test_sound.py` |
| `gamefx.py` | tellraw, частицы, звуки в игре | `test_gamefx.py` |
| `icons.py`, `uicons.py` | силуэты техники с LOD, иконки | `test_golden.py` |
| `maprender.py` | `MapTransform`, растр, слои | `test_maprender.py` |
| `app.py` | `Application` — композиционный корень | `test_application.py` |
| `ui/*` | Dear PyGui: цикл, проекция, карта, ПКМ | `test_ui_dpg.py`, `test_tac_state.py` |

---

## Требования

* Python 3.10+ (разработка и тесты — на 3.12)
* `dearpygui` 2.x — интерфейс; **ядро и тесты GL не требуют**
* `pillow` — иконка окна и работа с изображениями
* `pygame` — необязателен: звук деградирует до CLI/null-бэкенда
* `numpy` — необязателен, но заметно ускоряет растр рельефа и текстуры
* UI-слой собран по скиллу `skills/tactical-ui-dearpygui.md`
* Для офлайн-режима (имитатор сервера) больше ничего не нужно

```bash
pip install -r requirements.txt
```

## Как вести проект

| Файл | Назначение |
|------|-----------|
| `README.md` | этот файл — обзор, статус, модули, план |
| `ARCHITECTURE.md` | слои, инварианты, потоки, грабли DPG, точки расширения |
| `HANDOFF.md` | передача проекта: состояние, что сделано, что дальше |
| `BUGS.md` | реестр дефектов: баг · статус · сложность |
| `UX_AUDIT.md` | аудит интерфейса глазами оператора: находки с замерами и приоритетами |
| `CHANGELOG.md` | что сделано, по версиям и датам |
| `SKILL.md` | база знаний: RCON, Minecraft, версии, приёмы, соглашения |
| `skills/*.md` | методички: Dear PyGui-UI, рефакторинг |

Порядок работы над дефектом:

1. Строка в `BUGS.md` (ID, модуль, описание, статус `⬜`, сложность).
2. Тест, который падает на текущем коде.
3. Фикс → тест зелёный.
4. Статус `✅ исправлено`, запись в `CHANGELOG.md`.
5. Если по пути выяснилось что-то про Minecraft/RCON — в `SKILL.md`.

